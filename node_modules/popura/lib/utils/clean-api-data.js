'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol ? "symbol" : typeof obj; };

exports.default = cleanApiData;

var _flattenObject = require('./flatten-object');

var _flattenObject2 = _interopRequireDefault(_flattenObject);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * Cleans XML parsed JSON from MAL.
 *
 * @example
 * cleanApiData({ anime: { entry: [ { key: ['value'] } ] } })
 * [ { key: 'value' } ]
 *
 * cleanApiData({ manga: { entry: [ { key: ['value'] } ] } })
 * [ { key: 'value' } ]
 *
 * cleanApiData({ user: { key: ['value'] } })
 * { key: 'value' }
 *
 * @param  {object} data - MyAnimeList's API parsed XML
 * @return {object} - Good looking object
 */
function cleanApiData(data) {
  if (data === null) {
    return data;
  }

  var newData = data.anime || data.manga || data.user || data;

  if (Array.isArray(newData.entry)) {
    newData.entry.map(_flattenObject2.default);
    newData = newData.entry;
  } else if ((typeof newData === 'undefined' ? 'undefined' : _typeof(newData)) === 'object') {
    newData = (0, _flattenObject2.default)(newData);
  }

  return newData;
}
module.exports = exports['default'];